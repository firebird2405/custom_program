# 프로토콜 상태 — 캘린더 + 포스트잇 월
최종 갱신: 2026-08-21

## 목적 (확정문)
(승인: 2026-08-12 · rev.2 역반영 2조항[두 창 무손실·손상 복구] 포함)
1. 바탕화면 바로가기를 더블클릭하면 캘린더 창과 포스트잇 월 창이 각각 주소창 없는 독립 창(Edge 앱 모드)으로 열린다. 인터넷이 끊겨 있어도 동일하게 동작한다.
2. 포스트잇 월: "+ 새 포스트잇"을 누르면 노트가 생성되고, 내용 입력과 색상 선택(최소 4색)이 가능하다. 포스트잇을 드래그하면 커서를 따라오고, 놓은 위치에 고정된다. 앱을 껐다 재실행하면 모든 포스트잇이 같은 위치·색·내용으로 관찰된다.
3. 캘린더: v1(월간 그리드 · 2026-27 공휴일 · 일정 추가/수정/삭제)을 베이스로 유지·개선한다. 일정 추가 후 재실행하면 같은 날짜에 그 일정이 관찰된다.
4. 판정자: 무심한 실사용자(본인)의 일상 사용 + Playwright 헤드리스 실기 채점기(명령 한 줄로 전체 실행).
5. 이번 스코프: 위 1~4 + 실행 스크립트/바탕화면 바로가기까지. 검색·아카이브·알림 등 부가 기능은 이후 발주. 기존 memo.html(사이드바형)은 포스트잇 월로 대체.
6. 자원/마감: 로컬 npm 설치 허용(Playwright, 수백 MB). 오늘 v1 가동 목표.
7. 장식 에셋(코르크 질감·손글씨 폰트·아이콘 등): 인터넷에서 무료 라이선스 자료(CC0/퍼블릭 도메인/OFL 폰트 우선)를 내려받아 `assets/` 폴더에 로컬 번들한다. 실행 중 외부 네트워크 요청은 0건이어야 하며(오프라인 원칙 유지), 각 에셋의 출처·라이선스를 `assets/ASSETS.md`에 기록한다. 용도: 개인 사용 + **상업 배포 가능** (확정문 v2 개정 2026-08-18 — 전 에셋 상업 허용 라이선스 확인 완료).

## 현재 위치
- 단계: **출시 트랙(v2) 2주차 게이트 통과** (2026-08-19) — A45 3/3 · A47 2/2 · 전체 80 green/예외 0(잔여 red 3 = 3주차 몫: appx 매니페스트·개인정보처리방침·store-assets)
- **브랜딩 시사 완료**: 아이콘 3안 중 **A안(코르크 실사 감성) 사용자 선택** — 전체 MSIX 세트 재생성·적용 진행 중
- **Pro 개인키 보관**: `C:\Users\USER\.petitcalendar\keys\petit-private.pem` (+발급기 gen-license.js) — 저장소 밖 영구 위치로 복사 완료. 실판매 라이선스 발급용, 분실 시 공개키 교체 재배포 필요
- **소셜 로그인 검토 종결 (2026-08-19)**: 사용자 제안 → 검토 결과 정체성(오프라인·무계정·수집 0) 충돌·운영 부담으로 **철회 승인**. 대체 채택: **클라우드 폴더 백업**(백업 폴더를 OneDrive/드라이브 동기화 폴더로 지정 — 서버·로그인 0) + Pro 예약 자동 백업, 3주차 포함. 실시간 다중기기 동기화는 2차 게이트 후 재검토 카드로 백로그
- **3주차 게이트 통과 (2026-08-19)**: 전체 **82 green / 허용 red 1(A48② identity 대기) / 허용 SKIP 1(A50)** — 예외 0, 수정 라운드 0
  - PRIVACY.md(수집 0)·store-assets(연출 스크린샷 5장 + 설명문) — A48③④ green
  - 클라우드 폴더 백업 + Pro 예약 자동 백업(매일 1회, main측 서명 재검증 게이트) — 스모크 36/36, 회귀 21/21
  - .appx 준비 완료: identity 3개 값(Package/Identity/Name·Publisher·PublisherDisplayName)만 기입하면 `npm run dist:appx` — 가드 스크립트·[protocol/STORE-GUIDE.md](STORE-GUIDE.md) 사용자 가이드 완비
  - 부수 발견·수정: 빌더 화이트리스트에 preload/migrate/backup 누락(패키지 기동 불가였음) → 수정 + dist 재빌드·asar 포함 확인
- **4주차 대기 — 사용자 액션 1건**: MS 파트너센터 등록(~$19) → 제품 ID 3개 값 전달 (STORE-GUIDE.md 순서대로) → 이후 appx 빌드·A48②·A50 green·제출
- **1주차 게이트 판정: 통과** — 전체 84테스트 6.8분: **75 green / 8 red(전부 2·3주차 설계분: A45×2·A47×2·A48×4) / 1 SKIP(A50 MSIX 3주차)**. 기존 55개+1주차 항목+A12 전부 green, 플레이크 0 (A39 완화 유효). 현행 기준선 = 75/84

## 2단계 인프라 결과 (2026-08-19)
- **Electron 셸**: electron/ — main.js(A49 보안 기본선·창 상태 기억·최대 작업영역 디스플레이 첫 배치·믹스드 DPI 보정·세션 스펠체커 차단), preload.js(화이트리스트 브리지+이전 카드+온보딩 골격), migrate.js+migrate-dump.ps1(전 범위 마이그레이션 — IDB 포함·원본 불가침·멱등), electron-builder(25.1.8, fuse 유지, 개인 데이터 블랙리스트), 자체 제작 MSIX 아이콘 20종. win-unpacked 280.3MB ≤300MB
- **채점기 rev.6**: SCORECARD 병합 + 신규 스펙 9건(a42~a50, 84테스트/63파일) + electron-helpers + A39 완화(30/20→60초). 감사에서 병합 유실 크래시·fail-open 2건 복구. SHA **aef15ad1…** 이중 계산 일치 동결, A12 green, 잠금 복원
- **채점기가 잡은 실결함 2건 수정**: ① 스펠체커의 구글 CDN 사전 다운로드(A44 OS 레벨 감시 적발 → 창+세션 이중 차단), ② 두 번째 창 캐스케이드 화면이탈 크기 미달(A42 → 최대 디스플레이 명시 배치+DPI 보정)
- **1주차 게이트 타깃**: A42 8/8 · A43 2/2 · A44 3/3 · A46 1/1 · A49 3/3 전부 green. A45 1/3·A47 0/2·A48 2/6·A50 SKIP = **2·3주차 몫 설계상 red** (주차별 게이트 표 참조)
- rev.6 draft-2 핵심 결정(사용자 확인 필요): **Free 하한 = 2026-08-18 동결 빌드 실기능 전체**(사진·꾸미기·스티커 3세트 전부 무료 유지), **Pro = 순수 신규만**(신규 스티커 ≥2팩·프리미엄 스킨 ≥2종·보드 4개째부터·예약 자동 백업) — 레드팀이 잡은 "기존 기능 Pro 재포장" 빈틈과 A25/A26 동결 계약 모순을 동시에 해소하는 안
- **앱 이름 확정 (2026-08-18 사용자, 최종)**: 한글 **쁘띠캘린더** / 영문 **PetitCalendar** — 스위트명(캘린더+포스트잇 월 포함). 사용자 코멘트: 중의적이라 좋음. 2주차 브랜딩 발주에서: 스토어 이름 등록 가능 여부·상표 충돌 확인, 아이콘 디자인 반영
- 로컬 v1 트랙: 발주 #8까지 완료·실효 55/55 green, 시사 #2와 발주 #5/#6은 출시 트랙 1주차에 흡수 예정

## 확정문 v2 — 출시 트랙 (승인: 2026-08-18)
0. 전제: "1등"은 시장 결과라 직접 판정 불가 → **계단식 게이트**로 번역한다.
1. 기존 확정문 1~7(로컬 기능 계약)은 유지하되, 7조 "개인 사용, 상업 사용 없음" → "상업 배포 가능"으로 개정 (현 에셋 전부 상업 허용 라이선스 확인 완료 — CC0/OFL/MIT).
2. 패키징: Electron 전환 — 설치하면 시작 메뉴·바탕화면에서 자체 아이콘으로 실행, 두 앱이 독립 창으로 열리고, 기존 Edge 모드 사용자 데이터 마이그레이션 제공. 앱 런타임 외부 요청 0 유지(업데이트·결제는 MS Store 인프라 담당).
3. 차별화 축: 감성·꾸미기 — 첫 출시에 스티커 팩 확장·스킨 추가·"설치 후 60초 내 첫 꾸미기 완료" 온보딩이 관찰된다.
4. 수익 모델: COMMERCIAL.md 확정안 — Free(핵심 전부·광고 없음) + Pro 1회 구매(꾸미기 확장·보드 무제한·사진·자동 백업).
5. **1차 게이트(관찰 가능)**: MS Store 심사 통과·정식 출시 + 3개월 내 다운로드 1,000 · 평점 4.5+ · 리뷰 10건 → 달성 시 2차(순위 공략) 발주 개방.
6. 페이스: 4주 내 스토어 제출 — 1주차 Electron 셸+채점기 이식+지인 배포판(#6 흡수), 2주차 브랜딩(이름·아이콘)+온보딩+Pro 잠금, 3주차 스토어 등록·심사 요건(개인정보처리방침), 4주차 제출. 예산: MS 개발자 등록 ~$19 + 소액.
7. 채점 체계: 채점표 rev.6 개정 — Electron 기동·마이그레이션·Pro 잠금·패키지 크기 상한·심사 요건 체크리스트(A42~) 신설, 기존 55개는 회귀 게이트 유지. (개정 시 잠금 해제 1회 필요)

## 발주 #7 완료 (2026-08-18): 삭제 전 확인 + 보드 이름
- **삭제 전 확인**: 양앱 ⚙️ 설정 [안전] "삭제 전 확인" 토글(기본 꺼짐 — 채점 계약 보호, B조항 confirm() 금지 준수). 켜면 인앱 팝오버 "정말 삭제할까요? [삭제][취소]"가 파괴 동작 13곳(포스트잇 7 + 캘린더 6: 노트·보드·스티커·배경 제거·일정·반복 발생분/규칙·손상 백업)을 게이트. undo 토스트 병존(확인=실수 방지, undo=후회 방지). 문구·관용구 양앱 통일
- **보드 이름**: 탭 더블클릭 인라인 편집 + 보드 우클릭 "보드 이름 바꾸기", `postit-board-names` additive 키(merge-on-write + A9 손상 백업 규약), 빈 값=기본 이름 복귀, 최대 20자, 보드 삭제 undo 시 이름까지 복원
- 검증: 포스트잇 타깃 18/18 + 스모크 22/22, 캘린더 타깃 10/10 + 스모크 14/14, **전체 55/55 green**(양쪽 반영 상태, A39는 부하성 플레이크 단독 통과), SHA `32f23f0e…` 불변

## 발주 #8 (2026-08-18 사용자 지시): 창 크기·위치 기억
- 문제: 런처가 매 실행 고정 `--window-size/--window-position`을 강제 + Edge는 `--app` 창 배치를 스스로 복원하지 않음(실측 확인) → 켤 때마다 수동 크기 조절
- 구현: ① 런처 2종 — 크기 플래그를 **첫 실행(프로필 부재 시)에만** 부여, ② 양앱에 창 상태 모듈 — `cal-window`/`postit-window` additive 키에 실변경 시에만 기록(0.4s 디바운스+1.5s 이동 폴링+pagehide 즉시 기록), 시작 시 저장값 있으면 화면 클램프 후 복원(**최대 6회 재적용** — Edge의 늦은 기본 배치 덮어쓰기 대응). fresh 프로필=no-op(채점 회귀 보호), 테스트 환경=창 불변이라 기록 0건(A5 노이즈 무관)
- 검증: 실창 왕복(외부 이동→정상 종료→재실행) **오차 0px 복원** 확인. 진단 과정에서 확인된 사실: 150% 배율 환경의 CSS px 작업영역은 1280×680이며 Chromium이 스크립트 창 조절을 작업영역 안으로 클램프(화면보다 큰 저장값은 자동 축소 — 정상 동작)
- 전체 채점: 54/55 + A39 단독 통과(23.7s — 오늘 3회 연속 동일 패턴의 부하성 플레이크) → **실효 55/55 green**, 창 모듈 민감 항목(A0·A1·A4·A5) 전부 PASS, SHA 불변
- 참고: A39 부하성 플레이크가 반복 관찰됨(전체 실행 시 45초 타임아웃) — 채점기 수정 금지 원칙상 방치 중이나, 다음 채점기 개정 기회에 A39 타임아웃 완화 검토 후보로 기록

## 발주 #9 (2026-08-20 사용자 지시)
- **#9a 완료**: 보드 탭 우클릭 미니 메뉴(열기/이름 바꾸기/삭제) — 회귀 green, 커밋 47ffdd6
- **#9b 완료 — 1창 탭 모드 (2026-08-20)**: 병합 1창(기본)·탭 전환 무리로드(상태 보존)·분리/재병합·설정 영속(모드+기본 탭). WebContentsView 스택 방식, 탭바(tabbar.html+전용 preload), HTML 무변형. Edge 레거시 트랙(A13)은 2창 유지
  - 채점표 rev.7: A42 재정의(병합 계약) + a42 스펙 11테스트 재작성(무리로드 마커 증명 포함) + a49 탭바 검사 확대. **A42 11/11 · A49 3/3 green**, 셸 스모크 31/31
  - SHA 재동결: `9479bbe3…` (이중 계산 일치, 2026-08-20), 잠금 복원 완료. 최종 전체 게이트 실행 중

## 발주 #22~#31 완료 (2026-08-21) — 감사 Blocker 일괄 + 채점표 rev.9

감사 Blocker B1~B10 을 4개 무교차 파일 그룹으로 병렬 구현 후 전체 게이트 2라운드. **G0(#32)은 사용자 몫으로 남김.**

| 발주 | 결과 |
|---|---|
| #22 `✨정리` 매몰 | `planFlow()` 수용량 선계산 + y 클램프 삭제 + 간격 재시도(14→10→7→4) + 계단 띠. **완전 은폐 50장 28건 → 0건**, 정확 포갬 0, 보드 밖 0. 토스트 사실화("12장을 폈어요. 38장은 자리가 없어 겹쳐 뒀어요"), 되돌리기 20초(`ARRANGE_UNDO_MS`), hx/hy 보존 |
| #23 조용한 저장 실패 | 세션 1회 억제 폐지 → 30초 쿨다운. 양 앱에 지속형 배너 + [정리 방법 보기] → 설정 💾데이터. 80%/90% 선제 경고, IDB 무언 강등 제거. 신규 저장 키 0 |
| #24 백업 | 기본 폴더 `문서\쁘띠캘린더 백업`(설치 폴더 탈출 — MSIX 첫 클릭 실패 해소) + [📂 폴더 열기] + 1회 안내(`defaultNoticeAck` additive). **예약 자동 백업 무료화** |
| #25 Pro 잠금 OFF | `<html class="pro-hidden">` 단일 플래그로 `[data-pro-lock]`·`#dpProSec`·`#licensePop` 비노출, `FREE_BOARD_MAX` 게이트 제거(보드 무제한), 설정 검색 사전에서 Pro 항목 제거. **검증 코드·공개키·픽스처는 보존** — 재출시 때 클래스만 걷으면 복귀 |
| #26·#27 | 국문 리스팅 전면 개편(1창 2탭 정정·"무료·광고 없음·로그인 없음" 3곳·알림 조건 명시)·검색어 7칸 확정(17/21단어)·제출용 스크린샷 6장 선정. 영문 리스팅 ⛔보류(UI 한국어 단일), G1 을 KR 단독으로 환원 |
| #28 운영 장비 | 버전 정본을 `app.getVersion()` 하나로 통일(수동 상수 삭제), [📋 진단 정보 복사](메모 내용 미포함·외부 전송 0), 회전 로거 `userData\logs`(5×1MB), [🗂 로그 폴더 열기], main 크래시 훅. **문의처는 `main.js:310 CONTACT_PLACEHOLDER` 한 곳** — 아직 `{CONTACT}` 자리표시자 |
| #29 온보딩 | "노트 1장 이상이면 자동 종료"(새 저장 키 0, 기존 키에 `'auto'` 값만 추가) + 환영 카드 6줄→3줄 + 기본 창 1024×740 → **1280×860** |
| #30 항상 위 | 탭바 드로어 + 양 앱 설정, `shell-settings.json` additive `alwaysOnTop`, 재기동 복원 |
| #31 보드 자랑하기 | 활성 앱 뷰만 `capturePage`(탭바 미포함) → 클립보드 복사 / PNG 저장. **워터마크는 미포함**(별도 발주 필요 — 귀속 0건) |

### 채점표 rev.9 — 무료 단독 출시판
- **A45 전면 개정**: ① 무료 하한 = 동결 실기능 + **보드 무제한** + **예약 자동 백업 무료** ② **잠금 비노출 단언**(정상 조작 11단계 + 3초 지연 감시에서 `[data-pro-lock]`·`[data-license-import]` visible 0, 가격·구매 문구 0, 설정 검색 Pro 0행) ③ **라이선스 검증 코드 보존 의무**(공개키·WebCrypto verify·픽스처 — UI 노출은 금지) ④ Pro 상품 매니페스트 검사는 **A45-P 로 보류**
- B 금지 조항: 무료 후퇴 금지를 새 하한으로 확대 + "살 수 없는 잠금 재도입 금지"(Pro 재출시는 상품 신규성 A45-P + 실제 구매 동선을 함께 갖춘 뒤에만)
- SHA 재동결 **`7a5d22f1…`** (채점기 자기 출력 + 독립 PowerShell 이중 계산 일치), A12 green, 잠금(deny) 복원 완료

### 게이트
전체 88테스트 **86 green / 허용 red 1(A48② 파트너센터 identity 대기) / 허용 SKIP 1(A50 MSIX)** — 예외 0 (2026-08-21 7.9분 실측).
수정 라운드 2회: ① A45 rev.9 계약 이행(#24·#25) ② A13·A39 red 는 **앱 결함 아님** — `.edge\postit` LevelDB 오프셋 3089 손상(9일 새 4번째 재발)으로 확정, 복구 후 green.

## 항목별 계량 채점 (2026-08-21) — protocol/SCORE-AUDIT.md

감사를 17항목 가중 채점으로 계량화. 채점자 8명 → **도메인별 적대적 하향 검증 4명**(근거 file:line 재확인, "점수가 과하다"를 증명하는 방향) → 종합. **최종값은 검증관 값**이며 17항목 중 16항목이 하향됐다(평균 −6.4).

**가중 평균 45.5 → 발주 반영 후 51.8** (둘 다 "출시 전 수정 권고" 대역). 도메인: 제품·경험 51.3 / 기술·프로세스 52.3 / **신뢰성·운영 43.0** / **사업·시장 29.4 → 52.5**.

- 바닥: I13 수익모델 18 · **I2 재방문 유인 27** · I14 리스팅 28 · **I7 데이터 안전성 37(가중 11 — 최대)** · I16 확산 39
- 천장: I1 첫 90초 66 · I11 보안 65 · I3 핵심 루프 62 — **셋 다 70 미만**이라 자랑거리가 아니라 "감점이 아닌 자리"
- **구조적 결론**: 발주 #22~#31 은 "고장 수리 + 사업 정리"이지 제품 강화가 아니다. 6.3점 상승분 중 4.3점이 I9·I13·I14 즉 **코드가 아니라 문서·문자열**에서 나온다. Δ≤2 인 항목이 10개(가중 합 64/100)
- 하향의 공통 원인: 채점자는 "기능이 있는가"를, 검증관은 "그 기능이 실패할 때 사용자가 아는가"를 봤다

### 출시 판정 — "2주 연기" 유지 (사유는 교체됨)
원 연기 사유(Pro 잠금·리스팅 오류·백업 폴더)는 해소됐고, 그 자리를 **데이터 안전성 37·재방문 유인 27**이 대신한다. 조건부 출시 요건 7건은 SCORE-AUDIT.md §8.

**코드 몫 4건 해소 (2026-08-21 후속 발주 — 스모크 11/11 + 셸 회귀 6항목 green):**
1. ✅ 문의처 실주소 — `main.js CONTACT_PLACEHOLDER = uto2405@gmail.com`(PRIVACY.md §6 정본과 통일) + preload 폴백 + 스토어 설명문 문의줄 추가. `{CONTACT}` 리터럴 렌더 0 실증
2. ✅ 포스트잇 저장 실패 배너 — batch1 #23 에서 이미 구현돼 있었음(`notifySaveFailure` 호출 10곳 + 80/90% 선제 경고). 채점 당시 미반영 트리를 본 것
3. ✅ 자동 백업 — **1회 명시 선택 카드** 채택(기본 ON 은 A45 fresh 계약과 충돌: fresh 에서 "끔 → 토글 → auto:true" 를 실증하므로). 온보딩 종료·기동 시 좌하단 카드(z 99990 — 앱 모달 아래) "매일 자동 백업을 켜 둘까요?" → [켜 둘게요]=auto:true / [나중에]=ack 만 / 무응답 25초=접고 다음 실행에 다시. `backup-config.json` additive `autoPromptAck` + IPC `petit:backup:ack-auto-prompt`. 설정에서 직접 토글해도 답으로 기록
4. ✅ 온보딩 2단계 정지 — `targetUsable()` 가드(DOM 이탈·display:none·0크기)가 400ms 주기로 감시, 대상이 죽으면 같은 단계 재무장 + 2.5초 후 회복 문구("포스트잇을 한 번 눌러 편집 상태로…"). 재현 경로(무타이핑 blur) 실측: 회복 문구 → 재지목 → 타이핑 → 3/4 전진

남은 것 갱신 (2026-08-24):
- ✅ 스크린샷 국문 캡션 6장 — `store-assets/kr/` 생성(`tools/captionize-kr.js` 신설, en/ 파이프라인 재사용). 제출은 kr/ 판. 캡션 문구는 설명문과 같은 사실만, 브랜드 띠 "쁘띠캘린더 · 무료 · 광고 없음"
- ✅ 스토어 카테고리 **생산성 → 개인 설정(Personalization)** (STORE-GUIDE 정정 — LAUNCH-KIT 대원칙 "생산성 앱으로 소개하지 않는다"와의 문서 내 모순 해소, 감사 불일치 5건 중 마지막)
- 남은 **사람 일 2건**: **#32 G0 지인 검증** · 파트너센터 identity 3값.

## 전문가 감사 완료 (2026-08-21) — protocol/EXPERT-REVIEW.md
20년차 상업 유틸 페르소나 4렌즈(리텐션·지원지옥·수익화·포지셔닝) 실측 감사. **판정: 지금 내보내면 안 된다 — 2주 연기 권고**(고칠 것 대부분 공수 S).
핵심 통찰: **87테스트 85 green이 이번 발견 중 단 한 줄도 채점하지 않는다** — 채점기는 "만든 것이 안 깨졌는가", 출시는 "안 만든 것이 무엇인가"에서 결정된다.

### Blocker — 출시 전 필수 (발주 후보, 승인 대기)
| 발주 | 항목 | 공수 | 근거 |
|---|---|---|---|
| #22 | `✨정리` 매몰 수정 | S | postit.html:4882 — 50장 중 **28장 완전 은폐**인데 토스트는 "50개를 정리했어요" = 데이터 유실 리뷰 공장 |
| #23 | 조용한 저장 실패 제거 + 80/90% 선제 경고 | S | calendar.html:1041 세션 1회 후 무음 — 일주일치 일정이 조용히 미저장 |
| #24 | 백업 기본 폴더 이전 + [폴더 열기] + 자동 백업 상시화 | S | backup.js:59 기본 경로가 설치 폴더 → **MSIX 첫 클릭부터 실패** |
| #25 | Pro 잠금 전면 OFF·보드 무제한 | S | 구매 채널 0건(shell.openExternal 0)인데 잠금 UI만 노출 = 살 수 없는 결제 |
| #26 | 스토어 리스팅 사실 정합·무료 명시·검색어·스크린샷 교체 | S | "두 개의 아담한 창"(현행 1창)·합성 컷·'무료' 0회·검색어 0개 |
| #27 | 영문 리스팅 보류, G1을 KR 단독으로 | S | 앱이 한국어 단일(i18n 0) — 영어 리스팅은 1점 공장 |
| #28 | 버전 정본 통일·진단 정보 복사·문의처·로그/크래시 핸들러 | M | 사용자가 보는 버전이 `rev.5` — 버그 빌드 특정 불가, 앱 내 문의처 0건 |
| #29 | 온보딩 반복 차단·예시 보드·환영 카드 정리·창 1280×860 | S | preload.js:846/984 — 메모가 있는데 매 실행 "1/4 첫 장을 붙여 보세요" |
| #30 | 항상 위(alwaysOnTop) | S | 장르 최다 요청 기능 부재 — 무료 기본 앱에 패배 |
| #31 | 보드 자랑하기(PNG 복사·저장) | M | capturePage·ClipboardItem 0건 — 다꾸 앱인데 그림이 안 나온다(바이럴 훅 부재) |
| #32 | **G0 지인 검증 실행** | S | REVIEWS.md G0 기록 0건 — 9,900원이 고객 접촉 0회에서 나온 숫자 |

### D+30 후보
#33 트레이+OS 알림 승격+놓친 일정 회수(채점표 개정 동반, **재방문 트리거 현재 0건** — main.js:554 창 닫으면 프로세스 종료) · #34 휴지통 30일 · #35 손실 감지 가드·자동 스냅샷(LevelDB 손상 9일 3회 실측) · #36 보드 줌아웃(비겹침 수용 18장이 물리 상한) · #37 회수 품질·토스트 겹침 · #38 검색 매치 전면 배치 · #39 손상 백업 보기/내보내기 · #40 콜드 스타트 11.8초·유휴 545MB · #41 릴리스 노트·공휴일 만료 고지

### 수익 재검산 (감사 조정치)
첫해 현실 기대 **연 2만~20만원** — MARKET.md 기본 시나리오(6,000 DL)는 사실상 낙관, 보수가 실제 기본값. **투입 시간 상한 주 10시간** 권고, 라이선스 수동 발급 금지.

## 발주 #21 완료 (2026-08-21 사용자 버그 제보): 화면 밖 포스트잇 안전 회수
- **제보**: 4K에서 보드 전체에 붙인 뒤 2K로 내리면 약 1/4만 보이고 나머지는 접근 불가. 원인 확인 — 드래그 중에는 경계 클램프가 있으나 **저장분 렌더 시에는 클램프가 없어** 절대 좌표가 그대로 적용
- **채택 방식(사용자 선택)**: 안전 회수 + 원위치 기억. 판정은 기존 `onDragMove` 클램프 식을 재사용(=사용자가 드래그로 만들 수 있는 범위 안이면 무동작, 여유 2px) → 보드 안 노트는 좌표 절대 불변
- 회수 좌표는 통째로 보이도록 여유 있게 클램프(상대 위치감 유지, 자동 재배치 없음), 원좌표는 노트 additive 필드 `hx/hy`(신규 키 0)에 보존
- 복귀: 보드가 넓어져 원좌표가 통째로 들어오면 원자리로 되돌리고 `hx/hy` 삭제. 사용자가 직접 드래그·정리·가져오기 하면 기억 삭제(의사 우선)
- 알림: "화면 밖에 있던 메모 N장을 안으로 옮겼어요 · [실행 취소]" (복귀만 일어나면 무음)
- 채점 보호: 1280×800·1600×900 채점 좌표가 전부 안전 범위 안 → 무동작 실증, 리사이즈 훅은 300ms 디바운스 + 좌표 변경 0이면 저장 호출 자체 없음(A5 노이즈 방지)
- 검증: 스모크 21/21, 회귀 19항목 green, 전체 게이트 실효 **85 green / 예외 0**
- **부수 처리**: 채점 중 A13·A39 red → 원인은 캘린더 실사용 프로필 LevelDB 손상 재발(강제 종료 여파, 이전과 동일 증상). 수리 절차 재적용(데이터 9키 무손실 백업→격리→복원→왕복 검증) 후 둘 다 green. **재발 패턴이므로 채점기 개정 시 프로필 건강 검사 항목 승격 후보**

## 발주 #19·#20 완료 (2026-08-20 사용자 지시): 꾸미기 강화 2라운드
- **#19 스티커 회전 + 노트 앞 레이어**: 회전 손잡이(중심 기준 각도 추종, Shift 15° 스냅)·우클릭 ↺↻15°·초기화. 저장은 `rot`(부재=0) additive — 위치 정본은 계속 left/top, transform은 회전만 실어 스폰·겹침·클램프 계산 불변. 클램프는 **회전 AABB 기준**(rot=0이면 현행 식과 글자 그대로 동일 → 회귀 원천 차단). 리사이즈만 역회전 보정. **노트 앞 레이어**(`.sticker-layer-front` z9000, 노트의 형제라 가시성 판정 무영향) + 우클릭 토글 + 꾸미기 패널 기본 레이어 세그(기본=back). 스모크 42/42, 회귀 19항목 green
- **#20 파티클 효과 3종 + 전체 숨기기**(GIF 대신 벡터 — 선명·용량 0·조절 자유):
  - 반짝임 스티커 12종(`fx:'twinkle'` additive, 애니메이션은 안쪽 글리프에만 걸어 회전 transform 정본 보호)
  - 축포: 체크 1줄 완료 14개 / 보드 전체 완료 34개+토스트, DOM 파티클·레이어째 생성·소멸(평상시 잔여 0), 1회 ≤40·동시 ≤80. **노트 생성 축포는 의도적 제외**(pop-in+효과음+편집기 진입과 중첩, 최빈 동작이라 피로)
  - 분위기(캔버스 1장·rAF 1개, 이미지 0): 없음(기본)/반짝임/별빛/눈, 34~48개(상한 60), document.hidden 정지, reduced-motion·[효과] 끔 시 캔버스 미생성
  - **포스트잇 모두 숨기기**: 보드 우클릭 라벨 토글, `body.notes-hidden .pnote{display:none}` 한 줄 — 모델·저장 불변(저장 문자열 일치 실증), 스티커·배경은 유지. 숨김 중 상주 안내 칩([보이기]/[닫기])으로 갇힘 방지, **숨김 중 새 노트 생성 시 자동 해제**(혼란 방지 + 채점 보호)
  - 스모크 53/53, 회귀 24항목 green
- **자체 발견·수정한 회귀**: 꾸미기 패널에 행이 늘어 온보딩 드래그 대상(`#dpStSeason button`)이 1024×740 가시 영역 밖으로 밀려 A47① red → 팔레트 순서·문구 압축으로 y 632→547 복구(통과 구간 ≤568), 재발 방지 주석 삽입
- 게이트: **85 green / 예외 0** (dist 재빌드 1회 — 실행 중 앱 프로세스가 파일을 잠가 EPERM 발생, 정리 후 성공)

## 발주 #5·#6 + 스토어 자산 완료 (2026-08-20)
- **#5 IndexedDB 이미지 GC**: 보드 삭제 시 이미지 지연 sweep(undo 복원 시 보존), **보드 id 재사용 시 이전 배경 부활 버그 수정**(잔존물 선정리 방식 — id 규칙 유지가 A25 계약·정렬·기본 이름과 결합돼 있어 안전), 기동 1.5초 뒤 고아 청소(참조 재계산 후 삭제, 갓 올린 키 보호), 가져오기 교체 후 sweep, 내보내기 고아 미동봉. **스모크 40/40** + 패치 전 결함 4종 재현 실증(스모크 이빨 검증), 회귀 12항목 green
- **#6 배포판 생성**: `make_dist.ps1`(346줄) — 빌드 존재·최신성 확인 → **개인 데이터 블랙리스트 검사** → 스테이징+읽어주세요.txt → ZIP → **완성본 재검사** → 크기·SHA256 출력. 실행 검증: `dist_out\PetitCalendar-0.9.0-portable.zip` 117.9MB, 해제 후 블랙리스트 0건·exe 바이트 동일. 오염 시나리오 5건 차단 실증
- **스토어 자산**: 신규 스크린샷 7장(드래그 중·날짜 지정·연동 결과·설정 검색·온보딩 스포트라이트·연동 합성컷) + 영문 캡션판 9장 + **demo-drag.gif**(550×310·30프레임·458KB — ffmpeg 없이 PNG 디코더·median-cut 팔레트·프레임 차분·LZW를 순수 Node로 구현). 남은 격차: 세로 숏폼·mp4는 ffmpeg 필요
- 게이트: **85 green / 예외 0** (dist 재빌드 1회로 A42 정합)

## 발주 #18 완료 (2026-08-20 사용자 지시): 설정 자연어·태그 검색
- **설계 핵심**: 태그(보이는 것, 32개 하드캡·2~4자 명사·앱 간 문자열 동일)와 동의어(안 보이는 것, 무제한)를 **분리** — "자동시작·부팅·켤때·startup"을 전부 태그로 만들면 자동완성이 무의미해지는 문제 해소
- **매칭 엔진(전부 로컬, 외부 요청 0)**: NFC 정규화 → 조사·어미 스트리핑(은/는/이/가/시/때/으로…) → 쉼표 절 OR·공백 토큰 AND → 점수식(명칭 완전 100 > 접두 80 > 태그 70 > 동의어 65 > 접두 50 > 옵션값 35 > **초성 20**(ㅈㄷㅅㅈ→자동시작, 유니코드 분해로 로컬 계산) > 도움말 15, 오타 편집거리1 −25) → 카테고리·DOM 순 안정 정렬
- **# 태그 모드**: `#` → 전체 태그 가나다순(localeCompare ko), `#가` → 접두 필터, 선택 시 해당 태그 항목만, ↑↓·Enter·Esc 키보드
- **결과 리스트 방식 채택**(섹션 실시간 필터링 금지 — spApplyCat 클래스 계약·셸 MutationObserver·넘침 어포던스와 3중 경합 회피). 클릭 시 카테고리 이동+스크롤+펄스(reduced-motion·[효과] 이중 게이트)
- 사전: 포스트잇 36항목(브라우저 29)·태그 26, 캘린더 29항목(브라우저 25). 꾸미기 패널·툴바 항목까지 크로스 색인(포스트잇 꾸미기는 설정 밖이라 검색이 패널을 열어줌)
- 검증: 포스트잇 스모크 55/55, 캘린더 스모크 통과, **전체 게이트 86 green / 예외 0**(허용 red 1 = A48② identity 대기, SKIP 1 = A50)

## 발주 #17 완료 (2026-08-20 사용자 지시): 자동 시작 해제 + 설정 내 토글
- 사용자 요청으로 이 PC의 윈도우 시작 프로그램에서 캘린더·포스트잇 월 2건 제거(uninstall_startup.ps1) — 잔여는 사용자 개인 앱 3건뿐 확인
- **자동 실행 토글 신설**: `app.setLoginItemSettings` 내장 API(외부 프로세스 0), 상태의 단일 진실은 OS(파일 이중 저장 없음, 적용 후 재판독으로 조용한 실패 검출). 탭바 드로어 + 앱 설정 모달 양쪽에 토글, 한쪽 변경 시 즉시 상호 동기화, 실패 시 실측값으로 롤백+비모달 안내. 드로어 높이 112→138px 동기
- 검증: 스모크 19/19, 회귀 A42·A43·A44·A47·A49 green, 레지스트리 원복 확인(작업 전 기준선과 동일)

## rev.8 마감 (2026-08-20)
- 전체 게이트: **84 green / 2 red / 1 SKIP** — red는 A12(재동결 전)·A48②(identity 대기), SKIP은 A50. 재동결 후 A12 green 확인
- 채점기 SHA 재동결: `4999fc3d…` (Node·PowerShell 이중 계산 일치), 잠금(deny 2줄) 복원 완료

## 발주 #14~#16 완료 (2026-08-20): 설정 모달 · 스포트라이트 · 창 분리 폐지 · 마이그레이션 버그
- **#14 설정 창 중앙 모달**: 오른쪽 서랍 → **920×620 고정 프레임 + 딤 + 좌측 카테고리**(포스트잇 6·캘린더 7), 스크롤 2.0화면 → **전 카테고리 1화면**. 컨트롤 누락 0(훅 전수 대조, 삭제·개명 0건), 셸 주입 섹션은 MutationObserver로 카테고리 자동 배정. 스모크 72/72 + 셸 18/18 + 회귀 39항목 green
- **#15 온보딩 스포트라이트**: 대상 rect 실시간 추적 링(rAF, 오차 8px)+맥동+방향 포인터+옅은 딤, 드래그 단계 도착지·경로 힌트. reduced-motion·[효과] 시 정적 강등, pointer-events:none로 클릭 미간섭, 종료 시 전량 정리. 스모크 44/44
- **#16 창 분리 완전 폐지** (사용자 판단: "별로다 — 빼자"): 탭바 버튼·셸 설정 창 모드·preload 주입 [창 모드]·main.js 분리 경로(createAppWindow/runTransition/전환 IPC)·windowMode 개념 전부 삭제. **채점표 rev.8**: A42를 단일 창 계약으로 축소 + **5중 "분리 부재" 단언**(DOM·조작 후 지연 주입 감시·창 개수 폴링·구조·소스 잔존 금지 스캔) 신설. A42 11/11 green, 스모크 35/35
- **마이그레이션 버그 수정**: 패키지판이 레거시 프로필을 `win-unpacked\resources\.edge\`에서 찾던 문제(REPO_ROOT는 앱 리소스 뿌리) → 실사용 후보 탐색으로 교체, 실기동에서 감지·카드 표시 확인
- 사용자 실사용 제보 기여: "자동 가져오기 안 됨"(87테스트가 못 잡은 패키지 전용 결함), "창 분리 별로"

## 발주 #13 완료 (2026-08-20 사용자 지시): UIUX 직관성 재구성 + 튜토리얼 간소화
- **감사 실측**: 포스트잇 툴바 컨트롤 18개(첫 목표 "메모 남기기"에 필요한 건 1개=5.6%, 텍스트 라벨 없는 것 61%), 노트 우클릭 메뉴 398px=창 57%(대상 노트를 덮음), 설정 패널 2.4화면·정보 섹션이 31%, 캘린더 주 행동(추가 폼)이 화면 78% 높이·면적 2.8%에 격리 + 날짜 클릭 시 포커스 없음, 용어 3중 파편화(포스트잇 20/메모 33/노트 97)
- **재구성**: 툴바를 의미 4그룹(만들기·찾기와 보드·정리와 파일·모양과 소리)으로 위계화(최상위 자식 22→9, 폭 1234→1146px), 볼륨 슬라이더를 🔊 서랍으로 이동, `＋`→`＋ 보드`, 우클릭 메뉴 빈도·위험도 3구획 재정렬(398→320px)+파괴 항목 danger 색(대비 5.2:1/다크 5.6:1)·마지막 배치, 패널 스크롤 어포던스 이식, 캘린더 주 행동 승격·포커스 연결, 용어 통일
- **온보딩 6→4단계**: 자동 슬라이드 2개 제거(환영=빈 상태 카드와 중복, 축하=완료 토스트로 강등), 문구 한 단계 한 문장, fresh 첫 실행 대기 5.0s→0s, A47 실측 12.3s→7.5s. 대체 장치: 완료/건너뛰기 토스트 + "첫 우클릭" 1회성 힌트 칩(온보딩이 안 가르치게 된 부분 보완)
- 게이트: **85 green / 허용 red 1(A48②) / SKIP 1(A50)** — 예외 0, 수정 라운드 0 (A42는 dist 재빌드 1회로 정합)

## 발주 #12 완료 (2026-08-20 사용자 지시): 스크롤 어포던스 + 내용에 맞추기
- **실측 진단**: 발주 #11의 내부 스크롤로 휠은 이미 동작했으나 ① Edge 오버레이 스크롤바라 폭 0 = 시각 힌트 전무, ② 상단 드래그 스트립 위 휠 무반응 → "못 내린다"는 체감
- **어포던스**: `.ovf` 실측 클래스(scrollHeight-clientHeight>1)로 하단 페이드(mask-image)+▾ 힌트(CSS 삼각형 — textContent 불변)+`scrollbar-gutter: stable`(폭 8px 확보, 짧은 메모는 미적용). 노트 루트 wheel 위임으로 어느 지점에서든 스크롤, 끝 도달 시 기본 동작 통과
- **내용에 맞추기**(`[data-size-fit]`): 오프스크린 클론 실측 → 폭 유지 우선, 초과 시 240→300 완만 확대, 480 클램프+토스트. 좌상단 불변·즉시 저장
- **자동 맞춤 옵션**: 설정 [노트 기본값] "입력하면 자동으로 크기 맞추기"(기본 꺼짐, `postit-settings.autoFit`) — 편집 종료 시 조용히 적용
- 검증: 스모크 35/35, 회귀 A0·A2·A17·A18·A20·A21·A22·A23·A24·A25·A26·A28·A45·A47 + 인접 5그룹 green

## 발주 #11 완료 (2026-08-20 사용자 버그 제보 + 지시)
- **버그 수정**: 기본 노트 높이 auto 무한 성장 → 리사이즈 핸들이 화면 밖으로 나가 조절 불가였던 문제. `NOTE_MAX_PX`(480, 수동 리사이즈 상한과 동일 상수)를 CSS 변수로 내려 `.pnote { max-height }` 적용 + `.ntext` 내부 스크롤(코르크 톤). 기존 h>480 저장 노트는 sanitizeList의 clampSize가 이미 정규화함을 실측 확인
- **크기 프리셋**: 노트 우클릭 → 크기(작게150/보통210/크게300/아주크게420), 좌상단 불변·즉시 저장
- **다른 보드로 보내기**: 노트 우클릭 → 보드 선택(이름 반영, 1개면 비활성). 대상 병합 기록 성공 후 원 보드에서 제거(실패 시 원복), z는 대상 최상단, 위치 클램프. bid-aware 실행 취소(보드 전환 후에도 안전 — switchBoard의 토스트 소거를 boardSafe 플래그로 예외 처리), deletedIds 잔재 정리로 재이동 안전. 노트 사진은 n.img data URI라 이동 시 자동 동반
- 검증: 스모크 59/59, 채점기 회귀 지정 12그룹 + 인접 16그룹 전부 green

## 발주 #10 완료 (2026-08-20): 사진 스티커 배경 제거 + 셸 폴리시 패키지
- **누끼**: 업로드 직후 비모달 편집 카드(체커보드 미리보기·오차 슬라이더 8~64·흰 배경 토글) — 가장자리 연결 플러드필(모서리 중앙값 기준색, 피사체 내부 흰색 보존) + 페더링 2px, 결과 PNG→기존 IDB 경로, 우클릭 "배경 다듬기" 재편집 + undo. 스모크 30/30
- **셸 폴리시**(3차 점검 제안): 병합 모드 알림 릴레이(캘린더 백그라운드 시 탭 배지+미니 스트립), 재병합 섹션 설정 최상단, 탭바 손글씨 폰트, 0.18s 마이크로 모션(reduced-motion·[효과] 존중), 전환 무깜빡임 순서, Ctrl+Tab/1/2 단축키, 백업 백오프, 사어 스텁 정리. 스모크 15/15
- 게이트: **85 green / 허용 red 1(A48②) / SKIP 1(A50)** — 예외 0

## (원 발주 #10 사양 기록): 사진 스티커 배경 제거
- 사진 스티커 업로드 흐름에 간단 편집 단계 추가 — **흰 배경 날리기(누끼)**: 순수 캔버스 픽셀 처리(오프라인, 외부 API 0)
- 설계: ① 가장자리 연결 플러드필 방식(피사체 안쪽 흰색은 보존, 배경만 제거) ② 허용 오차 슬라이더 ③ 가장자리 부드럽게(페더링) ④ 체커보드 미리보기 + [배경 지우고 쓰기]/[그대로 쓰기] 선택 ⑤ 결과는 PNG(알파) data URI → 기존 IndexedDB 저장 경로 재사용
- 범위: postit.html 사진 스티커만(노트 사진 A26 계약 불변), 비모달 UI·textContent만, 동결 테스트 무간섭(additive)
- 채점기 SHA 독립 재계산: `32f23f0e…` 일치 (53파일, 5일 공백 무변조) · 잠금 deny 2줄 유지
- 전체 채점: 55개 중 54 PASS + A39 1건 실패 → **단독 재실행 통과(24s, 환경 플레이크** — 전체 실행 중 A13/A40 창 조작과 타이밍 충돌) → 실효 **55/55 green**
- 실사용 프로필: calendar·postit LevelDB 손상 기록 0건 (5일 실사용 후에도 재발 없음 — 우아한 종료 수정 유효)
- git push 완료 확인: main = origin/main @ 4f2117e (게이트 ① 해소)
- 백업 스냅샷 갱신: calendar/postit-20260818-102624.json

## 발주 #3·#3b·#4 결과 (2026-08-13)
- 발주 #3: ⚙️ 설정 패널 양앱(글자·주 시작 요일·음력/D-day 토글·알림 선행시간·효과·사운드 미러·데이터 관리·용량 게이지·손상 백업 정리·정보) + 자동 배치 5모드 + 점검 후속 수정 4건(전 보드 내보내기 v2, cal-repeats 백업 포함, 가져오기 비대칭 해소, 백업 키 가드 통일) — 55/55 green, C 대리 88.8
- 발주 #3b: 보드 삭제(실행 취소 토스트·기본/마지막 보드 보호) — 55/55 green
- 발주 #4: 🎨 꾸미기 — 배경 프리셋+사용자 이미지 업로드(IndexedDB postit-decor/cal-decor, localStorage 잠식 없음), 이모지 스티커 3세트(드래그·리사이즈), 사진 스티커(폴라로이드), 테이프/프레임 커스텀, 캘린더 배경 오버레이+강도 슬라이더, 내보내기 v2 decor 포함(이미지 동봉 옵션) — 55/55 green, 스모크 39/39
- 부수 성과: 포스트잇 실사용 프로필 LevelDB 손상도 치유(양 프로필 무손실 재생성), 관용구 통일(세그먼트·손상 백업 UI·게이지 문구)
- 진행 중 폴리시: 스티커/배경 삭제에 실행 취소 적용(유일한 undo 이탈 경로 봉합), 배경 상태 표기 통일, 프리셋 가시성, 스폰 산포, 포커스 접근성

## 발주 #1 결과 (2026-08-12)
- 자동 채점: **28/28 전 항목 PASS** (수정 루프 0회, 회귀 0회) · 채점기 SHA 무변조 확인 (d8abe994… 독립 재계산 일치)
- 종합 점수: **23항목 평균 97.35 / 목표 95 달성** — A1~A18 전부 100, C1 88 · C2 90 · C3 86 · C4 85 · C5 90 (C는 대리 점수, 시사로 확정)
- 산출물: postit.html(신규, 사운드·애니메이션 포함), calendar.html(비모달화·공휴일 수정·라이트 기본·사운드), assets/(CC0 코르크 + OFL 폰트 2종 + ASSETS.md), launch_postit.vbs·launch_all.vbs·make_shortcuts.ps1 갱신, 바탕화면 바로가기 2종(캘린더·포스트잇 월)
- 두 앱 실행 확인: launch_all.vbs → --app 창 2개 (calendar.html, postit.html) 기동 검증

## 발주 #2 결과 (2026-08-13)
- 기능 27건 전부 구현: 포스트잇(백업·undo·검색·정리·우클릭 메뉴·체크리스트·리사이즈·다중 보드·사진·스킨·포인터 드래그·메모 이전) + 캘린더(undo·D-day·카테고리·반복·알림·음력·주간 뷰·우클릭) + 공통(테마 프리셋 3종·볼륨) + 시스템(자동 시작 설치·백업 스냅샷·항상 위 핀) + 연동(포스트잇 날짜 지정→캘린더 일정)
- 채점표 rev.5 반영·채점기 동결: A항목 18→41개(스펙 23건 추가), SHA `32f23f0e…` 이중 계산 일치, A12 green. 잠금(deny 2줄) 복원 완료
- 사고 처리 2건: ① grader 내 떠돌이 Edge 프로필(해시 오염원) 격리, ② 실사용 캘린더 프로필 LevelDB 손상 수리 — 데이터 백업(backups/calendar-profile-rescue-*.json) 후 복원, 왕복 검증 통과
- 최종 전체 채점 (2026-08-13): **55/55 전 항목 PASS** (4.5분) — A1~A41 전부 green
- 종합 점수: **46항목 평균 98.83 / 목표 95 달성** — A 41항목 전부 100, C1 91 · C2 95 · C3 84 · C4 86 · C5 90 (대리 점수, 시사 #2로 확정)

## 발주 #3 (2026-08-13 사용자 지시): ⚙️ 설정 패널
- 두 앱에 톱니바퀴 버튼 → 슬라이드 설정 패널. 기능 구성은 Claude 추천안 위임.
- **공통**: 글자 크기(작게/보통/크게), 애니메이션 강도(켬/줄임/끔), 사운드 미러(음소거·볼륨), 데이터 관리(내보내기/가져오기 미러 + 저장 용량 게이지 + 손상 백업 키 정리), 정보(버전·에셋 라이선스·단축키)
- **캘린더**: 주 시작 요일(일/월), 음력 표시 ON/OFF, D-day 배지 ON/OFF, 알림 ON/OFF + 선행 시간(정각/5분/15분 전)
- **포스트잇**: 새 노트 기본 색·기본 크기, 기울임 정도(자연스럽게/반듯하게), 노트 폰트(손글씨/고딕)
- **자동 배치 (2026-08-13 사용자 추가 지시)**: 정리([data-arrange]) 방식 선택 — 현행 기본 / 3×3 격자 / 2×2 격자 / 동적 마소너리(텍스트·크기 맞춤) / 시간순. 설정 패널 '배치' 섹션 + 보드 우클릭 메뉴 연동. 기본값 = 현행 알고리즘(A28 회귀 보호), 상시 자동 재배치는 배제(자유 배치 원칙)
- **점검 후속 수정 포함**: ① 포스트잇 내보내기 전 보드 포함(+라벨 정확화, 가져오기 호환), ② 캘린더 내보내기에 반복 일정(cal-repeats) 포함, ③ 포스트잇 가져오기 기존 노트 필드 비대칭(w/h/skin/img/date) 해소, ④ 캘린더 손상 백업 키 충돌 가드 통일
- **보드 삭제 (2026-08-13 사용자 추가 지시)**: 보드 전환기·우클릭에 삭제 버튼, confirm 없이 즉시 실행+실행 취소 토스트(보드+노트 통째 복원, A20 패턴), 기본 보드·마지막 보드 삭제 불가. 설정 패널 워크플로우 완료 직후 같은 파일 충돌 없이 이어서 구현

## 발주 #4 (2026-08-13 사용자 지시): 🎨 꾸미기 기능 + 사용자 이미지 업로드
- **배경 커스텀**: 보드/캘린더 배경 — 기본 코르크 유지 + 프리셋 + **사용자 이미지 업로드**(다운스케일 후 적용, cover/타일 선택). 이미지는 IndexedDB(postit-decor) 저장 — localStorage 5MB 공유 한도 잠식 방지(점검 발견 대응). 기본(fresh) 상태는 현행 코르크 유지 → A10/A17/A36 회귀 보호
- **이모지 스티커**: 팔레트에서 골라 보드에 자유 배치·크기 조절·삭제, 노트 뒤 레이어(z), postit-decor 키 저장. 오프라인·에셋 불필요
- **사진 스티커**: 업로드 이미지를 스티커로 자유 배치 (노트 사진 A26 파이프라인 재사용, 저장은 IndexedDB)
- **노트 꾸밈**: 테이프 색/무늬 선택, 보드 프레임 색
- **진입점**: 🎨 버튼(설정과 분리, 과밀 방지) + 보드 우클릭 "꾸미기"
- **백업**: 꾸미기 레이아웃은 내보내기에 포함, 이미지 포함 여부는 체크박스(기본 꺼짐, 용량 경고)
- **제약**: 노트 사진(A26)은 현행 localStorage 유지(동결 테스트 보호), 스티커는 [data-note] 아님(채점 셀렉터 불간섭), 기존 55개 전체 회귀 green 필수
- **제약**: 기본값 = 현행 동작 유지(채점기 55개 테스트가 fresh 프로필 기본값 기준이므로 전부 green 유지 필수), 기존 훅([data-theme-preset]·[data-volume]·팔레트 등) 위치·가시성 불변(패널은 미러만), 신규 키 cal-settings/postit-settings(additive), textContent만, ESC 닫기
- **채점**: 기존 55개 전체 회귀 green + C 대리 채점. 설정 전용 A42 항목 신설은 다음 채점기 개정 기회에 묶어 처리(잠금 해제 최소화 — 후보로 기록)

## 사용자 액션 아이템
### 지금 해야 할 것
- [ ] **#32 G0 지인 검증** (사용자가 직접 수행하기로 함, 2026-08-21) — 질문 2개: "만원이면 사겠나" + **"그 만원에 뭐가 들어 있으면 사겠나"**(두 번째가 Pro 재구성의 유일한 입력값). 결과는 `protocol/REVIEWS.md` 에 원문 기록
- [ ] **문의처 실주소 확정** — `main.js:310 CONTACT_PLACEHOLDER` 한 곳만 채우면 앱·진단 텍스트 전부 반영. 지금은 설정창에 리터럴 `문의: {CONTACT}` 가 렌더된다(출시 차단)
- [ ] 파트너센터 identity 3값 (`protocol/STORE-GUIDE.md` 순서) → appx 빌드 → A48②·A50 green → 제출
- [ ] git push — `cd d:\custom_program` 후 `git push -u origin main --force` (firebird2405 계정 브라우저 인증, 최초 1회). 주의: backups\ 스냅샷에 실제 일정·메모 원문 포함 — private 저장소 전제, public 전환 시 .gitignore 추가 필요
- [ ] 시사 #2: 발주 #2~#4 완료본 판정 — 기능 27건 + 설정 패널 + 자동 배치 + 보드 삭제 + 꾸미기 (protocol/REVIEWS.md 시사 #2)

### 상업화 트랙 (2026-08-13 검토 완료 — protocol/COMMERCIAL.md)
- 모델 확정: 무료(광고 없음) + Pro 1회 구매(꾸미기 확장 중심), 광고 제외. 유통 1순위 MS Store
- 로드맵: 0단계 지인 배포 검증("만원이면 사겠는가") → 1단계 스토어 무료 출시 → 2단계 Pro
- **발주 #6 대기: 배포판 생성 스크립트** (개인 데이터 제외 ZIP + 경로 자동 설치 + 전용 아이콘) — "배포판 ㄱ"으로 시작

### 차기 발주 후보 (점검 2차, 2026-08-13)
- **#5 후보 (권장·최우선): IndexedDB 이미지 GC 패키지** — 보드 삭제/가져오기 시 이미지 sweep 누락(고아 잔존), **보드 id 재사용 시 삭제된 보드 배경이 새 보드에 부활(가시 버그)**, 기동 시 고아 청소, 내보내기 고아 동봉 방지
- #6 후보: 두 창 동기화 격차 — postit storage 리스너 부재, 꾸미기 레이아웃 last-writer-wins(스티커 단위 병합화)
- #7 후보: 캘린더 undo 완성(반복 삭제·배경 제거), 위생 일괄(죽은 코드·유틸 중복·문구 통일), 성능(saveNow 재파싱)은 후순위

### 완료됨 (시사)
- [x] 시사 #1 통과 (2026-08-12) — 결함 0건, "정상 동작 확인"
- [x] 발주 #2 확정 (2026-08-12) — 사용자 지시 "전부다 반영": BACKLOG 전체 27건 (P1~P12, C1~C8, S1~S7). S6은 Electron 전환 대신 Win32 핀 스크립트로 구현
- [x] 채점표 rev.5 개정 승인 (2026-08-12) — 발주 #2에 따른 신규 A항목(A19~A41) 추가 + 채점기 확장 + SHA 재기록. 사용자 지시에 근거한 정당 개정

### 루프 운영 규칙 (2026-08-12 사용자 지시로 추가)
- 자가 피드백 루프: 항목별 점수화 — A1~A18은 해당 테스트 통과율×100, C1~C5는 스크린샷 기반 대리 채점(0~100)
- **23항목 평균 ≥ 95점**까지 자율 반복 (구현→채점→수정, 3회 규칙 유지)
- C 항목 대리 점수는 잠정치 — 최종 확정은 사용자 시사(REVIEWS.md)로
- (2026-08-12 추가 지시) UIUX 효과 보강 라운드: 기본 구현 루프 완료 후 이펙트·애니메이션·사운드 구현 → C1~C5 재채점

### 완료됨
- [x] 인터뷰 질문 4개 답변 (2026-08-12) — 자유 배치 보드 / 별도 창 2개 / Playwright 채점 / 오늘 v1 가동
- [x] 목적 확정문 승인 (2026-08-12) — 에셋 조항(7) 포함
- [x] 채점표 v1 rev.2 승인 (2026-08-12) — 역반영 2조항(두 창 무손실·손상 복구) 확정문에 편입
- [x] 인프라 승인 (2026-08-12) — 권한을 .claude/settings.json에 적용, 채점기 첫 실행(10 PASS / 18 RED 전건 소명) 확인

## 단계 이력
- [x] 0단계 번역 인터뷰 — 2026-08-12 완료
- [x] 1단계 채점표 승인 — 2026-08-12 완료 (v1 rev.2)
- [x] 2단계 인프라 승인 — 2026-08-12 완료 (채점기 28테스트·SHA d8abe994…·레드팀 2종)
- [x] 3단계 루프 승인 — 2026-08-12 완료 (발주 #1 확정 + 95점 자가 피드백 루프 지시, 취향 앵커 기본값 ① 적용)

## 작업 루프 (선언)
1. 구현 → `cd protocol\grader && npm test` 실행 → 실패 시 원인 분석 후 수정 → 통과 시 변경 요약 보고
2. 3회 규칙: 같은 지점 3회 실패 시 중단하고 시도한 것·가설·막힌 지점 보고
3. 채점기·채점표 수정 금지(권한 deny + SHA 검증). 채점기 결함 발견 시 채점표 개정 절차로 승격(사용자 승인 필요)
4. 시사에서 사용자가 잡은 결함은 "왜 채점표가 못 잡았나"를 물어 조항으로 승격, 개정 이력에 기록

## 첫 발주서 초안

[발주 #1 — v1 가동: 포스트잇 월 신규 + 캘린더 결함 수정 + 런처·바로가기 갱신]
- 목표: 채점표 A1~A18 전 항목 green (현재 10/28 → 28/28)
- 완료 기준: `npm test` 28개 테스트 전부 PASS + 독립 해시 검증 유지
- 제약: CLAUDE.md 전 조항. 특히 —
  - 포스트잇 월(postit.html)은 채점기 스펙 상단의 DOM 계약([data-board]/[data-note]/[data-add-note]/[data-color]/[data-note-text]/[data-note-edit]/[data-delete-note], 단일 localStorage 키, confirm 금지) 준수
  - 캘린더 결함 수정: ① 가져오기 성공 alert(654행) → 페이지 내 비모달 안내로 교체(327·656·660행 alert도 함께 정리), ② 공휴일 목록에 2027-12-27 대체공휴일(성탄절) 추가 — cal-events 스키마·기존 일정 보존
  - 런처: launch_all.vbs가 calendar.html + postit.html을 열도록 갱신(포스트잇 전용 고정 프로필 .edge\postit), 바탕화면 바로가기를 캘린더·포스트잇 월 2종으로 갱신(구 메모장.lnk 정리). memo.html 파일 자체는 보존
  - assets/: 코르크 질감·손글씨 폰트 등 개인 사용 허용 무료 자료(CC0/PD/OFL 우선)만 다운로드해 로컬 번들, ASSETS.md 표(파일명|출처 URL|라이선스) 전수 기재, 런타임 외부 요청 0
- 취향 앵커: ① 코르크보드 + 파스텔 (기본값 — 변경 시 발주 확정 때 명시)
- 이 발주의 완료 기준과 무관한 파일 수정 금지 (스코프 크리프 차단)
- 산출물: postit.html(신규), calendar.html(결함 수정), assets/ + ASSETS.md, launch_all.vbs·launch_postit.vbs·make_shortcuts.ps1(갱신), 변경 요약 보고
- 막히면: 같은 지점 3회 실패 시 중단하고 시도한 것·가설·막힌 지점 보고

## 채점표 개정 이력
- v1 초안 (2026-08-12): A13항목·금지 6조·시사 5항·굿하트 4경로
- v1 초안 rev.2 (2026-08-12): 레드팀 3렌즈(게이밍/실현성/커버리지) 41건 반영 — A항목 13→18개(수정·삭제 A14/A18, 백업 왕복 A15, 바로가기 A16, 감성 스타일 A17 신설), 전 항목 관찰치를 computed style/폴링 기준으로 구체화, 채점기 변조 범위를 node_modules·실행 환경까지 확장, 굿하트 4→10경로
- v1 rev.3 (2026-08-12, 2단계 채점기 레드팀 반영): 개별 실행 형식을 콜론 형태(-g "A1:")로 개정(A1↔A10~A18 접두 충돌 해소), 채점기 SHA256 기대값·독립 검증 명령 기재, node_modules 사각지대 완화책(npm ci) 명문화. 채점기 측 강화: 환경 트립와이어(NODE_OPTIONS/.npmrc/PW_TEST_SOURCE_TRANSFORM), PowerShell 절대경로 고정, opacity·투명 글자 은닉 차단, A9 dialog 요구 제거(충실도 복원), A5 하트비트 노이즈 키 제외
- v1 rev.4 (2026-08-12, 사용자 지시): C5 'UIUX 효과' 항목 신설 — 마이크로 인터랙션·사운드(제스처 한정+음소거 토글)·reduced-motion 존중·드래그 중 위치 transition 금지 유지. 자동 항목(A)·채점기는 무변경(SHA 유지)
- v1 rev.5 (2026-08-13, 발주 #2 확장): A19~A41 신규 23항목(백업·undo·컨텍스트 메뉴·체크리스트·리사이즈·검색·다중 보드·사진·스킨·정리·메모 이전·반복·D-day·알림·음력·카테고리·주간 뷰·프리셋·볼륨·자동시작·백업 스냅샷·핀·연동), B 추가 단서 5개(데이터 URI 예외·backups/ 허용·스키마 보호 확장·시스템 스크립트 범위·전역 노출 제한), 굿하트 +5경로, SHA `32f23f0e…` 재동결. 개정 절차: 사용자 잠금 해제 → 반영 → 잠금 복원

- v1 rev.9 (2026-08-21, 무료 단독 출시판): A45 를 Free 경계 재정의로 전면 교체 — 무료 하한에 **보드 무제한·예약 자동 백업** 추가, **잠금 비노출 단언**(rev.8 A42 분리 부재 단언과 동형), **라이선스 검증 코드 보존 의무**(UI 노출은 금지), Pro 상품 매니페스트 검사는 **A45-P 로 보류**. B 에 "살 수 없는 잠금 재도입 금지" 신설. a45-free-pro.spec.js 4테스트 재작성. SHA `7a5d22f1…` 재동결(이중 계산 일치), 잠금 복원

## 열린 질문 / 보류 결정
- **취향 앵커 미지정** — 3단계 첫 발주 전까지 미지정 시 기본값 ① 코르크보드+파스텔로 진행 (①/②다크+비비드/③미니멀 화이트, 언제든 변경 가능).
- 기존 memo.html에 실제 메모를 이미 작성하셨다면 포스트잇 월로 데이터 이전 여부 결정 필요 (환영 메모뿐이면 불요).
